name: Test mpx workflow with yale primers

on: [pull_request, workflow_dispatch]

run-name: mpx_yale

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master

      - name: Clean up Disk space
        uses: jlumbroso/free-disk-space@main

      - name: Install Nextflow
        run: |
          wget -qO- get.nextflow.io | bash
          sudo mv nextflow /usr/local/bin/

      - name: Download reads
        run: |
          mkdir reads
          cd reads
          wget --show-progress=off ftp://ftp.sra.ebi.ac.uk/vol1/fastq/SRR256/074/SRR25601474/SRR25601474_1.fastq.gz
          wget --show-progress=off ftp://ftp.sra.ebi.ac.uk/vol1/fastq/SRR272/083/SRR27235483/SRR27235483_1.fastq.gz
          cd ../

      - name: Run Cecret
        run: |
          nextflow run . -profile docker,mpx_yale -c .github/workflows/github_actions.config --reads reads

          ls cecret*
            
          head cecret*/cecret_results.txt

      - name: Clean
        run: rm -rf work .nextflow*
